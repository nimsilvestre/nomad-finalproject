import React from 'react';
import { Menu } from 'semantic-ui-react';
import { browserHistory } from 'react-router';

const SearchBar = () => (
    <div>
        <Menu.Item href="/">MyFullFeaturesList</Menu.Item>
    </div>
);

export default SearchBar;
